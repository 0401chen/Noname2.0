# 第三阶段验证清单

该分支只用于触发干净环境验证，不包含额外产品功能。

验证项目：

- Python 3.11依赖安装；
- Ruff静态检查；
- Pytest单元与接口测试；
- 60个确定性对话场景；
- 完整系统与透明基线对比；
- React与TypeScript生产构建；
- 评测JSON作为Actions构件上传。

评测通过只表示项目满足预设工程和对话验收条件，不代表临床效果。
